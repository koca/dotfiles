[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[includeIf "gitdir:~/kod/onedio/"]
  path = ~/.me/me-work
[includeIf "gitdir:~/kod/junk/"]
  path = ~/.me/me-personal

